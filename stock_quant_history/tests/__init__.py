# Tests are intentionally not imported so they are not collected/run on
# Odoo.sh builds (--test-enable). The test module is kept in the repo and can
# be re-enabled by restoring the import below.
# from . import test_stock_quant_history
